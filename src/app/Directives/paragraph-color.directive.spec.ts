import { ParagraphColorDirective } from './paragraph-color.directive';

describe('ParagraphColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ParagraphColorDirective();
    expect(directive).toBeTruthy();
  });
});
